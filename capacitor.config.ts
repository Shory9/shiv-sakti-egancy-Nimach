import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shivshakti.executive',
  appName: 'Shiv Shakti Executive',
  webDir: 'dist'
};

export default config;
